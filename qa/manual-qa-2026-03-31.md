# Manual QA — 2026-03-31

## Environment

- Project path: `/Users/yeongyu/local-workspaces/not-claude-code-emulator`
- OAuth stub: `http://127.0.0.1:40123`

## 1. Normal login writes full XDG token JSON

Command:

```bash
XDG_DATA_HOME="/tmp/not-claude-code-login" ANTHROPIC_OAUTH_BASE_URL="http://127.0.0.1:40123" ANTHROPIC_OAUTH_CLIENT_ID="test-client-id" bun run src/cli.ts login --claudeai
```

Observed output:

```text
Paste code here if prompted > Opening browser to sign in…
If the browser does not open, visit: http://127.0.0.1:40123/oauth/authorize?code=true&client_id=test-client-id&response_type=code&redirect_uri=http%3A%2F%2F127.0.0.1%3A40123%2Foauth%2Fcode%2Fcallback&scope=user%3Aprofile+user%3Ainference+user%3Asessions%3Aclaude_code+user%3Amcp_servers+user%3Afile_upload+org%3Acreate_api_key&code_challenge=Cgik7Lnfd00rs7InpfZICDyDYdrWQGqXnbK5x_IRmb4&code_challenge_method=S256&state=9hOj7hyTKhw6u8FIYFZgx-zo1rXwgt4x
✅ OAuth login completed
Stored tokens at: /tmp/not-claude-code-login/anthropic/yeongyu/tokens.json
```

Stored file:

```json
{
  "accessToken": "login-access-token",
  "refreshToken": "login-refresh-token",
  "expiresAt": 1775004306877,
  "scopes": [
    "user:profile",
    "user:inference",
    "user:sessions:claude_code",
    "user:mcp_servers",
    "user:file_upload"
  ],
  "updatedAt": "2026-03-31T23:45:06.878Z"
}
```

## 2. setup-token prints raw token and writes oauthToken-only shape

Command:

```bash
XDG_DATA_HOME="/tmp/not-claude-code-setup" ANTHROPIC_OAUTH_BASE_URL="http://127.0.0.1:40123" ANTHROPIC_OAUTH_CLIENT_ID="test-client-id" bun run src/cli.ts setup-token
```

Observed output:

```text
Paste code here if prompted > Opening browser to sign in…
If the browser does not open, visit: http://127.0.0.1:40123/oauth/authorize?code=true&client_id=test-client-id&response_type=code&redirect_uri=http%3A%2F%2F127.0.0.1%3A40123%2Foauth%2Fcode%2Fcallback&scope=user%3Ainference&code_challenge=wdRFHSpjCZPgssG1RqOWeL6RL1bVpnoUxnbBL1PtEnw&code_challenge_method=S256&state=dZjO7-AblVqf9Jl8VeAr5kuY7KijGhJr
✅ OAuth login completed
Stored tokens at: /tmp/not-claude-code-setup/anthropic/yeongyu/tokens.json
Created long-lived token using the setup-token flow.
setup-access-token
```

Stored file:

```json
{
  "oauthToken": "setup-access-token",
  "updatedAt": "2026-03-31T23:45:06.920Z"
}
```

Permissions:

```text
0600
```

## 3. Conflicting login flags are rejected

Command:

```bash
bun run src/cli.ts login --console --claudeai
```

Observed output:

```text
Error: --console and --claudeai cannot be used together.
```

## 4. oauthToken-only file works for headerless localhost requests

Server command:

```bash
XDG_DATA_HOME="/tmp/not-claude-code-setup" ANTHROPIC_OAUTH_BASE_URL="http://127.0.0.1:40123" ANTHROPIC_OAUTH_CLIENT_ID="test-client-id" PORT=5456 HOST=127.0.0.1 bun run src/index.ts
```

Request command:

```bash
curl -sS -X POST "http://127.0.0.1:5456/v1/messages" -H "content-type: application/json" -d '{"model":"claude-sonnet-4-5-20250929","max_tokens":16,"messages":[{"role":"user","content":"hello"}]}'
```

Observed output:

```json
{"id":"msg_test","type":"message","role":"assistant","model":"claude-sonnet-4-5-20250929","content":[{"type":"text","text":"stub ok"}],"stop_reason":"end_turn","stop_sequence":null,"usage":{"input_tokens":1,"output_tokens":1}}
```

## 5. Verification commands

```bash
bun test
bun run typecheck
bun run build
```

Observed results:

- `bun test`: 89 pass, 0 fail
- `bun run typecheck`: success
- `bun run build`: success
